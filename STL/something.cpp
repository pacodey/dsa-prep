#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector <int> arr;
    int n;
    cout << "enter n: ";
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x;
        cout << "enter number: ";
        cin << x;
        arr.push_back(x);
    }
    
    for (int i = 0; i < n; i++)
    {
        for (int j = n - 1; j >= i; j--)
        {
            
        }
    }
}
